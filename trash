def sendUplet(uplet, s):

    
    uplet = [str(i) for i in uplet]
    length = len(json.dumps(uplet).encode())
    s.sendall(str(length).encode())
    s.recv(1)
    s.sendall(json.dumps(uplet).encode())
    s.recv(1)
